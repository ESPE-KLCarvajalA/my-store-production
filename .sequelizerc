// .sequelizerc
const path = require('path');

module.exports = {
  config: path.resolve('config', 'config.js'), // Ruta al archivo de configuración
  'models-path': path.resolve('db', 'models'), // Ruta a tus modelos
  'seeders-path': path.resolve('db', 'seeders'), // Ruta a tus seeders
  'migrations-path': path.resolve('db', 'migrations'), // Ruta a tus migraciones
};